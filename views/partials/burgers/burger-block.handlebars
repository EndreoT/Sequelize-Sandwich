<li class="list-group-item">

	<div class="row">



		<div class="col-7">
			<p>{{id}}. {{name}}</p>

		</div>


		<div class="col-5 text-right">
			{{#unless devoured}}
			<button class="btn btn-success active change-devoured" data-id="{{id}}" data-name="{{name}}">
				Devoure it!
			</button>
			{{/unless}}

			<button role="button"
				aria-pressed="true" class="btn btn-danger active delete-burger" data-id="{{id}}" data-name="{{name}}">Delete</button>

		</div>
	</div>
</li>